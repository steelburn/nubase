# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs
hs_err_pid*

# IDE
.idea/
*.iml
*.iws
.vscode/
.classpath
.project
.settings/

# Maven
/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Gradle
.gradle/
build/

# Spring Boot
spring-boot-*.log

# Application specific
logs/
# Local Spring profiles — keep secrets/dev configs out of the repo.
# Use application.yml as the canonical, sanitized template; place real values in
# any of these (or via env vars) and they'll never be committed.
application-local.yml
application-dev.yml
application-prod.yml
application-secrets.yml
*.pem
*.key
.env
.env.local
.env.*.local
.nubase/

# OS
.DS_Store
Thumbs.db
.postgrest.config

# Claude Code local settings
.claude/settings.local.json

# TypeScript incremental build info
*.tsbuildinfo

# Frontend build artifacts (with-frontend Maven profile)
frontend/.maven-frontend/
frontend/apps/*/out/

# Deploy secrets (the filled-in env lives only on the servers)
script/deploy/nubase.env

# Local machine config & build artifacts (never commit)
.mcp.json
frontend/packages/mcp-bridge/*.tgz
# Deploy script carries real prod origin IPs + APISIX admin key — keep local only
script/deploy/rolling-deploy.sh
# APISIX bootstrap carries real admin key + IPs — keep local only
script/deploy/init_apisix.sh

# www/APISIX deploy scripts carry prod origin IPs + APISIX admin key — keep local only
script/deploy/www-deploy.sh
script/deploy/apisix-www-routes.sh
/script/deploy/nubase-www.service
cloudflare/functions-dispatcher/wrangler.toml
cloudflare/functions-dispatcher/.wrangler/
